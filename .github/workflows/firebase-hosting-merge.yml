steps:
- uses: actions/checkout@v4 # This is the first step
        # ... (rest of checkout block)
        
      # The block below is causing the error. It is missing the hyphen AND is not indented correctly.
      # You need to find the correct insertion point in your full file and ensure it looks like this:
- name: Clean Install and Build # <-- NOTE THE HYPHEN HERE
      run: rm -rf ./node_modules && npm install && mkdir -p build && npm run build
      working-directory: temp-repo 

- uses: FirebaseExtended/action-hosting-deploy@v0 # <-- The deployment step starts here
        # ... (rest of deploy block)
