# .github/workflows/firebase-hosting-merge.yml
# This file was auto-generated by the Firebase CLI.
# https://github.com/firebase/firebase-tools
name: Deploy to Firebase Hosting on merge

on:
  push:
    branches:
      - main
# .github/workflows/firebase-hosting-merge.yml
# ... (rest of the file) ...

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
          fetch-depth: 0
          token: ${{ secrets.REPO_TOKEN }}
          # REMOVE: working-directory: temp-repo 
          
      # STEP 1: Install and Build (Still runs inside the submodule path)
      - name: Clean Install and Build
        run: |
          rm -rf ./node_modules
          npm install
          # FIX: Set the path for React to look in the current working directory (.) 
          # which is defined below as temp-repo
          REACT_APP_BUILD_PATH=. npm run build
        working-directory: temp-repo

      # STEP 2: Deploy (Uses the correct source path)
      # .github/workflows/firebase-hosting-merge.yml

# ... (rest of the file) ...

      # STEP 2: Deploy to Firebase Hosting
      - uses: FirebaseExtended/action-hosting-deploy@v1  # <-- CHANGE TO @v1
        with:
          repoToken: '${{ secrets.REPO_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_THE_AI_ADVISORY_BOARD_APP }}'  # <-- ADD THIS LINE
          projectId: the-ai-advisory-board-app
          # Remove or rename the 'source' input if it's there
